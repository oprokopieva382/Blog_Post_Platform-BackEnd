export type MeViewModel = {
  /**
   * Me email (required field & string)
   * Me  login (required field & string)
   Me userId (required field & string)
   */
  email: string;
  login: string;
  userId: string;
};
